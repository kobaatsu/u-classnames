for p in 0 1
  className = _utPrefix + -fxg + p
  .{className}
    flex-grow p
  .{className}-xs
    +xs()
      flex-grow p
  .{className}-sm
    +sm()
      flex-grow p
  .{className}-md
    +md()
      flex-grow p
  .{className}-lg
    +lg()
      flex-grow p
  .{className}-xl
    +xl()
      flex-grow p
